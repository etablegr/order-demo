{
  "name": "on_connect",
  "version": "1.0.0",
  "description": "AWS Lambda for srtoring the connection_id of api webwsocket gateway",
  "main": "app.js",
  "scripts": {
    "run": "sam invoke local "
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mysql": "github:mysqljs/mysql"
  },
  "devDependencies": {
    "sam": "0.0.7"
  }
}
